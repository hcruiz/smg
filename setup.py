from setuptools import setup, find_packages

setup(name='smg',
      version='0.0.0',
      description='Surrogate models generation of dopant-network devices.',
      url='',
      author='Hans-Christian Ruiz Euler',
      license='GPL-3.0',
      packages=find_packages(),
      install_requires=[],
      python_requires='~=3.8',
      zip_safe=False)
